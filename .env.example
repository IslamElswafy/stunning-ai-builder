# Server-side only. Never prefixed with NEXT_PUBLIC_, never sent to the browser.

# Required. Anthropic API key — https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Optional. Overrides the model used by the generate route.
# Defaults to claude-opus-5 (see lib/ai.ts).
AI_MODEL=
